import React, { useState } from "react";
import Link from "@docusaurus/Link";
import { useLocation } from "@docusaurus/router";

# QnA

{(() => {
  const questions = [
                      {
                        id: 1,
                        question: "What is EliteFit.AI?",
                        icon: "help_outline",
                        answer: [
                          "EliteFit.AI is an AI-powered physiotherapy and fitness platform that delivers real-time feedback through computer vision.",
                          "It helps clinics, physiotherapists, and individuals improve rehabilitation outcomes while also supporting preventive care."
                        ],
                        tips: [
                          "Use EliteFit.AI consistently to track progress.",
                          "Try integrating it with your existing rehab or fitness routine."
                        ],
                        cautions: [
                          "Always consult a healthcare professional before starting new exercises.",
                          "Avoid overexertion—follow the recommended sets and reps."
                        ]
                      },
                      {
                        id: 2,
                        question: "How does AI feedback work?",
                        icon: "help_outline",
                        answer: [
                          "The system uses computer vision to analyze posture and movements in real time.",
                          "It provides instant corrective feedback, ensuring safe and effective exercises."
                        ],
                        tips: ["Ensure proper lighting and camera placement for accurate tracking."],
                        cautions: ["Incorrect setup may reduce accuracy of feedback."]
                      },
                      {
                        id: 3,
                        question: "How do I record a Console Log?",
                        icon: "help_outline",
                        answer: [
                          "Console logs help us diagnose issues that aren’t visible on screen. Follow these steps:"],
                        list: [
                          "In Google Chrome:",
                          [
                          "Open the page where the issue happens.",
                          "Press Ctrl+Shift+J (or Cmd+Option+J on Mac) to open Developer Tools.",
                          "Click the Console tab.",
                          "Reproduce the issue to capture any errors.",
                          "Right-click in the console → Save as... → Download the .log or .txt file.",
                          "Attach this file when submitting your issue."],
                          "Optional – Network Logs (Advanced):",
                          ["In Developer Tools, go to the Network tab.",
                          "Enable Preserve log.",
                          "Reproduce the issue.",
                          "Right-click anywhere in the network panel → Save all as HAR with content.",
                          "Share the .har file with us if requested.",
                          "These logs are optional but very helpful for identifying hard-to-reproduce issues."]
                        ],
                        tips: ["Capture logs immediately after reproducing the issue for accuracy."],
                        cautions: ["Do not share logs publicly—they may contain sensitive information."]
                      },
                      {
                        id: 4,
                        question: "How do I find my Session ID?",
                        icon: "help_outline",
                        answer: [
                          "Your Session ID is typically found in the workout URL or through your browser’s developer tools (if exposed).",
                          "From the URL after completing a workout:",
                        ],
                        list: [
                          "Example: https://www.elitefitforyou.com/user/end-workout/v2?sessionId=63ef9b27-5ea2-45ae-8faa-4a5aef66e172",
                          ["Here, the Session ID is: 63ef9b27-5ea2-45ae-8faa-4a5aef66e172",
                          "From the Developer's Console during an active workout:",
                          "Example: session id ::: 53ef9b27-5ea2-45af-8faa-4a5aef66e172"],
                          "Here, the Session ID is: 53ef9b27-5ea2-45af-8faa-4a5aef66e172",
                          "By checking the browser’s address bar on the workout summary or end page."
                          ],
                        tips: ["Take a screenshot or copy the full URL if you can’t locate the Session ID."],
                        cautions: ["Do not share your Session ID in public forums for security reasons."]
                      },
                      {
                        id: 5,
                        question: "Where can I find the Video ID?",
                        icon: "help_outline",
                        answer: [
                          "The Video ID (sometimes shown as the workout ID or video title) is part of the workout URL.",
                          "From the workout URL when active:",
                          "Example: https://www.elitefitforyou.com/workout/PremiumFeedback?id=348458",
                          "The Video ID here is: 348458",
                          "By checking the browser’s address bar when the workout is active.",
                          "If you can’t find it, share the full workout URL with our team—we’ll extract the Video ID for you."
                        ],
                        tips: ["Bookmark the workout link for easy retrieval of the Video ID."],
                        cautions: ["Double-check the ID before sharing it with support to avoid confusion."]
                      }
                    ]


  // Grab query param "id"
  const location = useLocation();
  const params = new URLSearchParams(location.search);
  const id = params.get("id");
  const [activeId, setActiveId] = useState(id);

  if (id) {
    const q = questions.find((q) => +q.id === +id);
    if (!q) return <p className="text-gray-600">Question not found.</p>;

    // Full QnA page (not drawer)
    return (
      <div>

        {/* Full-width icon */}
        <div className="w-full h-[50vh] flex items-center justify-center bg-green-50 dark:bg-gray-800 mb-6 rounded-lg">
          <span className="material-icons text-green-600 dark:text-green-50" style={{ fontSize: "20rem", opacity: "0.7" }}>
            {q.icon}
          </span>
        </div>

        {/* Question */}
        <h2 className="text-xl font-semibold text-gray-900 dark:text-gray-100 mb-4">
          {q.question}
        </h2>

        {/* Answer paragraphs */}
        <div className="space-y-4 text-gray-700 dark:text-gray-300 text-left mb-6">
          {q.answer.map((para, idx) => (
            <p key={idx} className="leading-relaxed">
              {para}
            </p>
          ))}
        </div>

        {/* Professional Nested List */}
        {q.list && (
          <div className="mt-6 p-4 bg-gray-50 dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-700">
            <ul className="space-y-3 text-gray-700 dark:text-gray-300">
              {q.list.map((item, idx) =>
                typeof item === "string" ? (
                  <li key={idx} className="font-medium mt-3">
                    {item}
                  </li>
                ) : Array.isArray(item) ? (
                  <ul
                    key={idx}
                    className="ml-6 list-decimal space-y-1 text-gray-600 dark:text-gray-400"
                  >
                    {item.map((subItem, subIdx) => (
                      <li key={subIdx} className="leading-relaxed">
                        {subItem}
                      </li>
                    ))}
                  </ul>
                ) : null
              )}
            </ul>
          </div>
        )}



        {/* Tips */}
        {q.tips && (
          <div className="mt-6 p-4 bg-blue-50 dark:bg-blue-900 rounded-lg border-l-4 border-blue-500">
            <h3 className="font-semibold text-blue-700 dark:text-blue-300 mb-2">
              Tips
            </h3>
            <ul className="list-disc list-inside space-y-1 text-gray-700 dark:text-gray-300">
              {q.tips.map((tip, idx) => (
                <li key={idx}>{tip}</li>
              ))}
            </ul>
          </div>
        )}

        {/* Cautions */}
        {q.cautions && (
          <div className="mt-6 p-4 bg-red-50 dark:bg-red-900 rounded-lg border-l-4 border-red-500">
            <h3 className="font-semibold text-red-700 dark:text-red-300 mb-2">
              Cautions
            </h3>
            <ul className="list-disc list-inside space-y-1 text-gray-700 dark:text-gray-300">
              {q.cautions.map((caution, idx) => (
                <li key={idx}>{caution}</li>
              ))}
            </ul>
          </div>
        )}
      </div>
    );
  }else{
  return (
      <div className="flex flex-col divide-y divide-gray-200 dark:divide-gray-700">
        {questions.map((q) => (
          <div key={q.id} className="mb-6">
            {/* Question header */}
            <button
              onClick={() => setActiveId(activeId === q.id ? null : q.id)}
              className="w-full flex items-center justify-between p-4 hover:bg-gray-50 dark:hover:bg-gray-800 rounded-lg transition-colors"
            >
              <span className="text-gray-900 dark:text-gray-100 font-medium group-hover:text-green-600 transition-colors">
                {q.question}
              </span>
              <span className="material-icons text-gray-400 group-hover:text-green-600 transition-colors">
                {activeId === q.id ? "expand_less" : "chevron_right"}
              </span>
            </button>

            {/* Drawer content */}
            {activeId === q.id && (
              <div className="mt-4 p-4 rounded-lg border border-gray-200 dark:border-gray-700">
                {/* Short preview of answer */}
                <p className="text-gray-700 dark:text-gray-300 mb-4">
                  {q.answer[0]}
                </p>
                <Link
                  to={`?id=${q.id}`}
                  className="text-green-600 hover:underline font-medium"
                >
                  Read full answer →
                </Link>
              </div>
            )}
          </div>
        ))}
      </div>
    );
  }
})()}
